<Window x:Class="NutriPlanner.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Вход в систему" FontSize="20" Margin="0 0 0 20" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="Логин"/>
            <TextBox Text="{Binding Username}" Margin="0 0 0 10"/>
            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" Margin="0 0 0 10"/>
        </StackPanel>

        <Button Content="Войти" Command="{Binding LoginCommand}" Grid.Row="2" Margin="0 0 0 10"/>
        <Button Content="Регистрация" Command="{Binding OpenRegisterCommand}" Grid.Row="3"/>
    </Grid>
</Window>
