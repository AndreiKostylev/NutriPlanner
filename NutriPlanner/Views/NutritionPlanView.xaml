<UserControl x:Class="NutriPlanner.Views.NutritionPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Создание плана питания (для диетологов)" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <!-- Выбор клиента -->
        <GroupBox Grid.Row="1" Header="Выбор клиента" Margin="0,0,0,10" Padding="10">
            <StackPanel>
                <TextBlock Text="Выберите клиента:" Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding Clients}" 
                          SelectedItem="{Binding SelectedClient}"
                          DisplayMemberPath="Username"
                          Width="200"/>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Загрузить клиентов" 
                            Command="{Binding LoadClientsCommand}"
                            Margin="0,0,10,0"
                            Padding="10,5"/>

                    <Button Content="Рассчитать цели" 
                            Command="{Binding CalculateTargetsCommand}"
                            Padding="10,5"
                            Background="#4CAF50"
                            Foreground="White"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- Основная информация плана -->
        <GroupBox Grid.Row="2" Header="Настройка плана" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Левая колонка - основные параметры -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Основные параметры:" FontWeight="Bold" Margin="0,0,0,10"/>

                    <StackPanel>
                        <TextBlock Text="Название плана:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding PlanName}" Margin="0,0,0,10"/>

                        <TextBlock Text="Период действия:" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal">
                            <DatePicker SelectedDate="{Binding StartDate}" 
                                        Width="120" 
                                        Margin="0,0,10,10"/>
                            <TextBlock Text="-" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <DatePicker SelectedDate="{Binding EndDate}" 
                                        Width="120" 
                                        Margin="0,0,0,10"/>
                        </StackPanel>

                        <TextBlock Text="Статус:" Margin="0,0,0,5"/>
                        <ComboBox SelectedItem="{Binding Status}" Margin="0,0,0,10">
                            <ComboBoxItem Content="Активен"/>
                            <ComboBoxItem Content="Неактивен"/>
                            <ComboBoxItem Content="Черновик"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>

                <!-- Правая колонка - целевые показатели -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Целевые показатели:" FontWeight="Bold" Margin="0,0,0,10"/>

                    <StackPanel>
                        <TextBlock Text="Калории (ккал/день):" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TargetCalories}" 
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Белки (г/день):" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TargetProtein}" 
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Жиры (г/день):" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TargetFat}" 
                                 Margin="0,0,0,10"/>

                        <TextBlock Text="Углеводы (г/день):" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TargetCarbs}" 
                                 Margin="0,0,0,10"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Рекомендуемые блюда -->
        <GroupBox Grid.Row="3" Header="Рекомендуемые блюда" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Список блюд -->
                <DataGrid Grid.Column="0" 
                          AutoGenerateColumns="False"
                          ItemsSource="{Binding RecommendedDishes}"
                          SelectedItem="{Binding SelectedDish}"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Название" 
                                            Binding="{Binding DishName}" 
                                            Width="*"/>
                        <DataGridTextColumn Header="Калории" 
                                            Binding="{Binding TotalCalories}" 
                                            Width="70"/>
                        <DataGridTextColumn Header="Белки" 
                                            Binding="{Binding TotalProtein}" 
                                            Width="70"/>
                        <DataGridTextColumn Header="Жиры" 
                                            Binding="{Binding TotalFat}" 
                                            Width="70"/>
                        <DataGridTextColumn Header="Углеводы" 
                                            Binding="{Binding TotalCarbohydrates}" 
                                            Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Кнопки управления -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <Button Content="Добавить в план" 
                            Command="{Binding AddDishToPlanCommand}"
                            Margin="0,0,0,10"
                            Padding="10,5"
                            Width="120"/>

                    <Button Content="Удалить из плана" 
                            Command="{Binding RemoveDishFromPlanCommand}"
                            Margin="0,0,0,10"
                            Padding="10,5"
                            Width="120"/>

                    <Button Content="Создать план" 
                            Command="{Binding CreatePlanCommand}"
                            Margin="0,0,0,10"
                            Padding="15,8"
                            Width="120"
                            Background="#4CAF50"
                            Foreground="White"
                            FontWeight="Bold"/>

                    <Button Content="Сгенерировать план" 
                            Command="{Binding GenerateMealPlanCommand}"
                            Padding="10,5"
                            Width="120"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>