<UserControl x:Class="NutriPlanner.Views.NutritionPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="План питания" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

       
        <DataGrid Grid.Row="1" AutoGenerateColumns="False"
                  ItemsSource="{Binding PlanItems}" 
                  SelectedItem="{Binding SelectedPlanItem}"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Вес (г)" Binding="{Binding Weight}" Width="70"/>
                <DataGridTextColumn Header="Калории" Binding="{Binding Calories}" Width="70"/>
                <DataGridTextColumn Header="Белки" Binding="{Binding Proteins}" Width="70"/>
                <DataGridTextColumn Header="Жиры" Binding="{Binding Fats}" Width="70"/>
                <DataGridTextColumn Header="Углеводы" Binding="{Binding Carbs}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

       
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <ComboBox Width="200" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" DisplayMemberPath="ProductName"/>
            <TextBox Width="50" Margin="5,0" Text="{Binding SelectedWeight}" />
            <Button Content="Добавить" Command="{Binding AddProductCommand}" Margin="5,0"/>
            <Button Content="Удалить" Command="{Binding RemoveProductCommand}" Margin="5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
